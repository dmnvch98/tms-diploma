server:
  port: 9090

spring:
  datasource:
    driver-class-name: 'org.postgresql.Driver'
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:35432/diploma

avatar:
  user_postfix: '_avatar.png'
find-tutor-page-size: 5
services:
  conversation:
    url: 'http://${CONV_SVC_HOST:localhost}:9093'
roles:
  student_role_name: 'Student'
  tutor_role_name: 'Tutor'
logging:
  level:
    sql: trace